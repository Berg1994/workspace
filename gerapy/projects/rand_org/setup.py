# Automatically created by: gerapy
from setuptools import setup, find_packages
setup(
    name='rand_org',
    version='1.0',
    packages=find_packages(),
    entry_points={'scrapy':['settings=rand_org.settings']},
)